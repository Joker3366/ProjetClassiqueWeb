@model IEnumerable<ProjetClassiqueWeb.Controllers.Genre>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Libelle_Abrege)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Libelle_Abrege)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Code_Genre }) |
                @Html.ActionLink("Details", "Details", new { id=item.Code_Genre }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Code_Genre })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
