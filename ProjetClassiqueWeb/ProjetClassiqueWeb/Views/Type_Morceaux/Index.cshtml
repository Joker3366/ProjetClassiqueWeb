@model IEnumerable<ProjetClassiqueWeb.Controllers.Type_Morceaux>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Libelle_Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Libelle_Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Code_Type }) |
                @Html.ActionLink("Details", "Details", new { id=item.Code_Type }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Code_Type })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
